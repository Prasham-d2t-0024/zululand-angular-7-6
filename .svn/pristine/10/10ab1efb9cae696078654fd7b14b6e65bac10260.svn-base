<!-- <div class="chart-container">
  <ngx-charts-line-chart *ngIf="!isLoading"
    [view]="view"
    [scheme]="colorScheme"
    [legend]="false"
    [showXAxisLabel]="showXAxisLabel"
    [showYAxisLabel]="showYAxisLabel"
    [xAxis]="xAxis"
    [yAxis]="yAxis"
    [xAxisLabel]="xAxisLabel"
    [yAxisLabel]="yAxisLabel"
    [timeline]="timeline"
    [results]="chartOptions">
  </ngx-charts-line-chart>

  <div class="custom-legend">
    <div *ngFor="let series of chartOptions; let i = index" class="legend-item">
      <span class="color-box" [style.backgroundColor]="colorScheme.domain[i % colorScheme.domain.length]"></span>
      {{ series.name }}
    </div>
  </div>
</div> -->
<ngx-charts-line-chart *ngIf="!isLoading"
  [scheme]="colorScheme"
  [legend]="legend"
  [showXAxisLabel]="showXAxisLabel"
  [showYAxisLabel]="showYAxisLabel"
  [xAxis]="xAxis"
  [yAxis]="yAxis"
  [xAxisLabel]="xAxisLabel"
  [yAxisLabel]="yAxisLabel"
  [timeline]="timeline"
  [results]="chartOptions"
  [legendPosition]="legendPosition"
  [showGridLines]="false"
  [xAxisTicks]="xAxisTicks">
</ngx-charts-line-chart>
<!-- <div class="custom-legend">
  <span *ngFor="let series of chartOptions" class="custom-legend-item">
    <span class="legend-color" [style.backgroundColor]="series.color"></span>
    {{ series.name }}
  </span>
</div> -->
<div class="d-flex justify-content-center align-items-center" style="min-height: 450px;" *ngIf="!!isLoading">
  <div class="spinner-border" role="status">
    <span class="sr-only">{{'home.label.loading' | translate}}</span>
  </div>
</div>