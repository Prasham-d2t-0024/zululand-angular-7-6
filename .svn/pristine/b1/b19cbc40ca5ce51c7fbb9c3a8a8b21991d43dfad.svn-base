import { ResourceType } from "../resource-type";

/**
 * The resource type for WORKFLOWPROCESSE
 *
 * Needs to be in a separate file to prevent circular
 * dependencies in webpack.
 */
export const CHART_FORMAT = new ResourceType('items');
