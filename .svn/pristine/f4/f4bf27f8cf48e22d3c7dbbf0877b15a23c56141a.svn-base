import { ComponentFixture, TestBed } from '@angular/core/testing';

import { HomeTredingGeolocationComponent } from './home-treding-geolocation.component';

describe('HomeTredingGeolocationComponent', () => {
  let component: HomeTredingGeolocationComponent;
  let fixture: ComponentFixture<HomeTredingGeolocationComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ HomeTredingGeolocationComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(HomeTredingGeolocationComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
